// Exporting a function so it can be used in other files
export function addNumbers(a: number, b: number): number {
    return a + b;
}

// Exporting a class
export class Calculator {
    multiply(a: number, b: number): number {
        return a * b;
    }
}